# .github/workflows/ping-render-cron.yml
name: Randomized Ping Render App

on:
  schedule:
    # Every 5–13 minutes approximately by setting multiple offsets
    - cron: "0,7,12,18,23,30,37,42,48,53 * * * *"
    - cron: "3,10,15,21,26,33,40,45,51,58 * * * *"

jobs:
  ping-render:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: |
          echo "Pinging Render app at $(date)"
          curl -s https://locomoco.top
